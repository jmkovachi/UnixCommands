#include <cstdlib>

/**
This is the main method of the function, it will return 0 on
success but it is made to return 1 (failure) every time.
@return Returns 0 on success, but will always fail and return 1.
*/
int main()
{
	return EXIT_FAILURE;
}